# --- API base ---
base.uri=https://gorest.co.in
base.path=/public/v2

# --- Auth (Bearer token) ---
# Replace Primary_Token with your actual token from gorest.co.in
auth.token=Bearer Primary_Token

# --- Timeouts ---
timeout.ms=10000
